@using LexCalculus.Core.Calculators.Common
@using LexCalculus.Core.Calculators.IsHukuku
@model IseIadeInput
@{
    var meta = (CalculatorMetadata)ViewData["Meta"]!;
    var result = ViewData["Result"] as IseIadeResult;
}

<div class="calc-header">
    <h1 class="calc-header__title">@meta.Title</h1>
    <div class="calc-meta">
        <div>№ <strong>@meta.DisplayNumber</strong></div>
        <div>@meta.LegalReference</div>
        <div>@meta.Category.ToShortName().ToUpper()</div>
    </div>
</div>

<section class="hero hero--inline">
    <p class="hero__lead hero__lead--narrow">@meta.ShortDescription</p>
</section>

<div class="info-box info-box--info">
    <strong>Bu hesap ne zaman kullanılır?</strong>
    <ul style="margin: var(--sp-2) 0 0 var(--sp-4); padding: 0;">
        <li>İşçinin işveren tarafından geçersiz olarak işten çıkarıldığını iddia ettiği davalarda</li>
        <li>Mahkemenin feshin geçersizliğine karar verdiği durumlarda</li>
        <li>İşverenin işçiyi yeniden işe başlatmadığı senaryolarda</li>
        <li>İş güvencesi hükümlerinden yararlanan işçiler için (30+ işçi, 6+ ay kıdem)</li>
    </ul>
    <div style="font-size: var(--fs-xs); margin-top: var(--sp-3); opacity: 0.8;">
        Fesih bildiriminden itibaren <strong>1 ay</strong> içinde dava açılmalıdır (m.20).
    </div>
</div>

<div class="info-box info-box--warning">
    <strong>İş güvencesi kapsamı şarttır.</strong> Otuz işçili işyerinde, 6+ ay kıdemi olan, belirsiz süreli sözleşmeli işçinin haksız feshe karşı m.18-21 talep hakkı vardır. Hesap kapsam dışında da yapılır ancak uyarı verilir.
</div>

<div class="calc-layout">
    <form method="post" class="form-section" asp-controller="Hesapla" asp-action="IseIade">
        <h2 class="form-section__title">Hesaplama Bilgileri</h2>

        <div asp-validation-summary="ModelOnly" class="info-box info-box--danger" role="alert"></div>

        <div class="field-row">
            <div class="field">
                <label asp-for="GirisTarihi" class="field__label field__label--required">
                    @Html.DisplayNameFor(m => m.GirisTarihi) <span class="required">*</span>
                </label>
                <input asp-for="GirisTarihi" class="field__input" type="date" />
                <span asp-validation-for="GirisTarihi" class="field-hint"></span>
            </div>

            <div class="field">
                <label asp-for="FesihTarihi" class="field__label field__label--required">
                    @Html.DisplayNameFor(m => m.FesihTarihi) <span class="required">*</span>
                </label>
                <input asp-for="FesihTarihi" class="field__input" type="date" />
                <span asp-validation-for="FesihTarihi" class="field-hint"></span>
            </div>
        </div>

        <div class="field">
            <label asp-for="KararTarihi" class="field__label field__label--required">
                @Html.DisplayNameFor(m => m.KararTarihi) <span class="required">*</span>
            </label>
            <input asp-for="KararTarihi" class="field__input" type="date" />
            <div class="field-hint">Mahkemenin işe iade kararı verdiği tarih (boşta geçen süre hesabı için).</div>
            <span asp-validation-for="KararTarihi" class="field-hint"></span>
        </div>

        <div class="field">
            <label asp-for="BrutAylikUcret" class="field__label field__label--required">
                @Html.DisplayNameFor(m => m.BrutAylikUcret) <span class="required">*</span>
            </label>
            <input asp-for="BrutAylikUcret" class="field__input" type="number" step="0.01" min="0" placeholder="örn. 30000" />
            <span asp-validation-for="BrutAylikUcret" class="field-hint"></span>
        </div>

        <div class="field-row">
            <div class="field">
                <label asp-for="IadeAyiSayisi" class="field__label">
                    @Html.DisplayNameFor(m => m.IadeAyiSayisi)
                </label>
                <input asp-for="IadeAyiSayisi" class="field__input" type="number" step="1" min="4" max="8" />
                <div class="field-hint">Mahkeme takdir eder; ortalama 4-5 ay.</div>
                <span asp-validation-for="IadeAyiSayisi" class="field-hint"></span>
            </div>

            <div class="field">
                <label asp-for="IsyeriCalisanSayisi" class="field__label">
                    @Html.DisplayNameFor(m => m.IsyeriCalisanSayisi)
                </label>
                <input asp-for="IsyeriCalisanSayisi" class="field__input" type="number" step="1" min="1" />
                <div class="field-hint">İş güvencesi için minimum 30 işçi şartı vardır.</div>
            </div>
        </div>

        <button type="submit" class="btn btn--primary btn--block">Hesapla</button>
    </form>

    <div class="result-panel">
        <h3 class="result-panel__title">Sonuç</h3>

        @if (result is null)
        {
            <div class="result-panel__empty">
                Formu doldurup "Hesapla" butonuna basarak işe iade tazminatı hesabını görüntüleyebilirsiniz.
            </div>
        }
        else
        {
            <div class="result-total">
                <div class="result-total__label">@result.TotalLabel</div>
                <div class="result-total__amount">
                    @result.TotalAmount?.ToString("N2", new System.Globalization.CultureInfo("tr-TR"))<span class="result-total__currency">@result.Unit</span>
                </div>
            </div>

            <div class="result-rows">
                @foreach (var row in result.Rows)
                {
                    <div class="result-row">
                        <span class="result-row__key">@row.Key</span>
                        <span class="result-row__val">@row.Value</span>
                    </div>
                }
            </div>

            @foreach (var warning in result.Warnings)
            {
                <div class="result-note">@warning</div>
            }

            @if (!string.IsNullOrWhiteSpace(result.Note))
            {
                <div class="result-note">@Html.Raw(result.Note)</div>
            }
        }
    </div>
</div>

<div class="catalog-back-link">
    <a class="btn btn--ghost" href="/hesapla/is-hukuku">← İş Hukuku</a>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
