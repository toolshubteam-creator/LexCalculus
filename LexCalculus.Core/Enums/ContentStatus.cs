namespace LexCalculus.Core.Enums;

/// <summary>
/// Publication lifecycle status for content entities (pages, posts).
/// Used in Phase 3 by the admin panel content editor.
/// </summary>
public enum ContentStatus
{
    Draft = 0,
    Published = 1,
    Archived = 2
}
